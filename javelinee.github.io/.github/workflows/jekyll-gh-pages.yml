# This workflow is intentionally disabled.
# Deployment is handled by pages.yml (Flutter web static build).
name: Deploy Jekyll with GitHub Pages dependencies preinstalled

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Jekyll deployment disabled. Using pages.yml for Flutter web deployment."
