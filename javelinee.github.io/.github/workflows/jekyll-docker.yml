# This workflow is intentionally disabled.
# Deployment is handled by pages.yml (Flutter web static build).
name: Jekyll site CI

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Jekyll CI disabled. Using pages.yml for Flutter web deployment."
